# Deployed backend API, including the /api suffix.
VITE_API_URL=https://your-backend.vercel.app/api

# Optional: Socket.IO host deployed outside Vercel (Render/Railway/etc.).
# Leave empty if realtime chat is not configured yet.
VITE_SOCKET_URL=
